var gallons = parseFloat(prompt("Enter Amount in gallons."));

var oz = (gallons * 128);

alert (("Converts to ")+(oz)+("ounces"));

console.log(gallons);
console.log(oz);

// var  unitType = prompt ("Enter units.");
